const Header = () => {
  return (
    <header style={{ backgroundColor: 'darkRed' }} className="text-white">
        <h2 className="p-2">Funds Dashboard</h2>
    </header>
  );
};

export default Header;